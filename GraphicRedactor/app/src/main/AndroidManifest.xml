<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.graphicredactor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GraphicRedactor">
        <activity android:name=".spline.SplineActivity"></activity>
        <activity android:name=".scaling.ScalingActivity" />
        <activity android:name=".rotating.RotatingActivity" />
        <activity android:name=".retouching.RetouchingActivity" />
        <activity android:name=".masking.MaskingActivity" />
        <activity android:name=".filtration.FiltrationActivity" />
        <activity android:name=".filter.FilterActivity" />
        <activity android:name=".facerecognition.FaceRecognitionActivity" />
        <activity android:name=".cube.CubeActivity" />
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>